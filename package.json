{
  "name": "unihack-desktop",
  "version": "2.0.0",
  "description": "Native-first cross-platform security tool orchestration workbench",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "prepare:sidecars": "node scripts/prepare-sidecars.mjs",
    "build": "node scripts/prepare-sidecars.mjs && tauri build",
    "build:nsis": "tauri build --bundles nsis",
    "build:msi": "tauri build --bundles msi",
    "build:installer": "tauri build --bundles nsis,msi",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
