# Rust Compilation Fixes for main.rs

## Summary of Issues:
1. AppHandle::current() doesn't exist - need to pass app_handle through functions
2. read_stream needs execution_id and step_id parameters
3. AppState needs Clone trait
4. Unused imports and variables

## Required Changes:

### 1. Add app_handle parameter to execute_workflow_async
Line 233:
```rust
async fn execute_workflow_async(
    state: AppState,
    app_handle: tauri::AppHandle,  // ADD THIS
    execution_id: String
) -> Result<(), String> {
```

### 2. Pass app_handle to execute_dag
Line 255:
```rust
execute_dag(execution, &workflow_template, app_handle).await?;  // ADD app_handle
```

### 3. Update execute_dag signature
Line 260:
```rust
async fn execute_dag(
    execution: &mut WorkflowExecution,
    template: &WorkflowTemplate,
    app_handle: tauri::AppHandle  // ADD THIS
) -> Result<(), String> {
```

### 4. Replace all AppHandle::current() with app_handle parameter
Lines 284, 314, 328, 340, 364, 373 - replace:
```rust
tauri::AppHandle::current().emit_all(...)
```
with:
```rust
app_handle.emit_all(...)
```

### 5. Pass app_handle to execute_step
Update all calls to execute_step to include app_handle

### 6. Update execute_step signature to accept app_handle
Add app_handle parameter to function

### 7. Update read_stream calls
Pass execution_id, step_id, and app_handle to read_stream

All `AppHandle::current()` calls need to be replaced with the `app_handle` parameter passed through the call chain.

